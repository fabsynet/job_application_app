---
phase: 06-playwright-browser-submission-learning-loop
plan: 08
type: execute
wave: 4
depends_on: ["06-05", "06-06", "06-07"]
files_modified:
  - tests/integration/__init__.py
  - tests/integration/test_phase6_requirements.py
autonomous: false

must_haves:
  truths:
    - "SC-1: Playwright fills and submits a Greenhouse/Lever/Ashby form end-to-end with profile data and storageState persistence"
    - "SC-2: Unknown fields halt the application in needs_info with full context (job, question, field type, screenshot)"
    - "SC-3: User answers once and retry succeeds without creating a duplicate"
    - "SC-4: Semantically similar questions auto-filled from cached answers"
    - "SC-5: Generic ATS forms submitted via the same label-matching engine with per-step screenshots"
  artifacts:
    - path: "tests/integration/test_phase6_requirements.py"
      provides: "Integration tests for all 8 Phase 6 requirements"
      contains: "test_sc"
  key_links:
    - from: "tests/integration/test_phase6_requirements.py"
      to: "app/playwright_submit/strategy.py"
      via: "PlaywrightStrategy end-to-end invocation"
      pattern: "PlaywrightStrategy"
    - from: "tests/integration/test_phase6_requirements.py"
      to: "app/learning/matcher.py"
      via: "LLM semantic matching verification"
      pattern: "find_matching_answers"
---

<objective>
Comprehensive integration tests validating all Phase 6 success criteria and requirements, plus a human verification checkpoint for the complete Playwright + learning loop flow.

Purpose: Prove every Phase 6 requirement is met through automated tests that exercise the full stack (mocked Playwright, mocked LLM, real DB). The human checkpoint is the phase-end visual sweep.
Output: Integration test file + checkpoint for visual verification.
</objective>

<execution_context>
@C:\Users\abuba\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\abuba\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-playwright-browser-submission-learning-loop/06-RESEARCH.md
@.planning/phases/06-playwright-browser-submission-learning-loop/06-05-PLAN.md
@.planning/phases/06-playwright-browser-submission-learning-loop/06-06-PLAN.md
@.planning/phases/06-playwright-browser-submission-learning-loop/06-07-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration tests for all Phase 6 requirements</name>
  <files>tests/integration/__init__.py, tests/integration/test_phase6_requirements.py</files>
  <action>
  Create `tests/integration/__init__.py` (empty, if not exists).

  Create `tests/integration/test_phase6_requirements.py` with comprehensive tests. Use the existing `async_session` and `live_app` conftest fixtures. Mock Playwright's `async_playwright()` at the module level (no real browser). Use FakeLLMProvider for semantic matching tests.

  **SUBM-03 Tests (Playwright submits to Greenhouse/Lever/Ashby):**
  - `test_sc1_greenhouse_end_to_end`: Create a greenhouse job + profile + tailoring record. Mock Playwright page interactions (navigate, fill, submit). Invoke PlaywrightStrategy.submit(). Verify success outcome, Submission row created with submitter="playwright".
  - `test_sc1_lever_end_to_end`: Same for lever source.
  - `test_sc1_ashby_end_to_end`: Same for ashby source.
  - `test_sc1_storage_state_persisted`: After successful submit, verify save_state() was called (storageState survives restarts per SUBM-05).

  **SUBM-04 Tests (Generic ATS forms):**
  - `test_sc5_generic_ats_submission`: Create a job with unknown source. Mock page with a form containing 3+ inputs. Verify GenericFiller selected, fields classified, form submitted, per-step screenshots captured.
  - `test_sc5_generic_form_detection`: Mock page with multiple forms, verify the one with file input is selected.

  **SUBM-05 Tests (Persistent session):**
  - `test_subm05_storage_state_loaded`: BrowserManager loads storageState when file exists.
  - `test_subm05_storage_state_created`: BrowserManager saves storageState after submission, creates dir if missing.

  **LEARN-01 Tests (Unknown field logging):**
  - `test_sc2_unknown_fields_halt_needs_info`: Create a job where form has unknown fields. Invoke submit with pause_if_unsure=True. Verify job flips to needs_info, UnknownField rows created with field_label, field_type, screenshot_path, page_number, is_required.
  - `test_learn01_unknown_field_context`: Verify UnknownField rows include job context (via FK), field_type correctly detected.

  **LEARN-02 Tests (User reviews in web UI):**
  - `test_learn02_needs_info_queue_page`: GET /needs-info returns the halted job with unknown field count.
  - `test_learn02_needs_info_detail_page`: GET /needs-info/{job_id} returns field labels, screenshots, and answer form.

  **LEARN-03 Tests (Answers persisted and reused):**
  - `test_sc3_answer_and_retry_no_duplicate`: Answer unknowns via POST /needs-info/{id}/answer. Verify SavedAnswer rows created. Trigger retry. Verify job flips to submitted (or approved, depending on mock). Verify NO duplicate Submission row if retried again (idempotency from 05-01 partial UNIQUE index).
  - `test_learn03_answers_in_saved_answers_ui`: After answering, GET /settings/saved-answers shows the new answers.

  **LEARN-04 Tests (Semantic matching):**
  - `test_sc4_semantic_match_auto_fills`: Create a SavedAnswer for "Are you authorized to work in the US?". Submit a new job with field "Work Authorization". Mock LLM to return the match. Verify the field is auto-filled (not unknown), SavedAnswer.times_reused incremented.
  - `test_learn04_no_match_stays_unknown`: Mock LLM returns NONE. Verify field remains unknown.

  **LEARN-05 Tests (Retry halted application):**
  - `test_sc3_immediate_retry_after_answer`: POST /needs-info/{id}/retry triggers Playwright submission inline. Verify immediate response (not scheduled).

  **Cross-cutting:**
  - `test_registry_order`: default_registry returns [PlaywrightStrategy, EmailStrategy].
  - `test_playwright_falls_through_to_email`: Non-ATS job with email recipient -> EmailStrategy selected.
  - `test_captcha_halts_with_screenshot`: Mock CAPTCHA detected -> needs_info with error_class="captcha".
  - `test_fail_fast_on_error`: Playwright timeout -> failed with error screenshot.
  - `test_reused_answers_in_success_email`: After auto-match submit, success notification includes reused answer summary.

  Use monkeypatching for Playwright (mock async_playwright, page, locator). Use FakeLLMProvider for LLM calls. Use real DB via async_session fixture.
  </action>
  <verify>Run `python -m pytest tests/integration/test_phase6_requirements.py -v`</verify>
  <done>All Phase 6 requirement tests pass. SUBM-03/04/05 and LEARN-01/02/03/04/05 covered. SC-1 through SC-5 verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 6 Playwright submission + learning loop</what-built>
  <how-to-verify>
  Phase-end visual sweep — verify in a browser at http://localhost:8000:

  1. **Settings > Playwright**: Toggle headless/headed, pause_if_unsure, screenshot retention visible with correct defaults
  2. **Settings > Saved Answers**: Empty state "No saved answers yet" shown initially
  3. **Needs Info page** (/needs-info): Empty state "No halted applications" when no jobs in needs_info
  4. **Navigation**: "Needs Info" link visible in nav bar between Review and Applied
  5. **General**: All existing pages (Dashboard, Jobs, Review, Manual Apply, Applied, Settings) still render correctly — no visual regressions from Phase 6 additions

  Note: Full Playwright form-filling cannot be visually verified without a real ATS form. The integration tests verify the logic via mocked Playwright. The visual sweep confirms the UI components render correctly.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- All integration tests in test_phase6_requirements.py pass
- Full project test suite passes with zero regressions
- Phase-end visual sweep confirms UI renders correctly
</verification>

<success_criteria>
SC-1: Playwright form submission end-to-end (mocked) for all three ATS platforms.
SC-2: Unknown fields halt with full context.
SC-3: Answer + retry without duplicate.
SC-4: Semantic matching auto-fills similar questions.
SC-5: Generic ATS via same engine with screenshots.
All 8 requirements (SUBM-03/04/05 + LEARN-01/02/03/04/05) verified by integration tests. Visual sweep approved.
</success_criteria>

<output>
After completion, create `.planning/phases/06-playwright-browser-submission-learning-loop/06-08-SUMMARY.md`
</output>
