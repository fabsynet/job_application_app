---
phase: 06-playwright-browser-submission-learning-loop
plan: 07
type: execute
wave: 3
depends_on: ["06-01", "06-04"]
files_modified:
  - app/web/routers/saved_answers.py
  - app/web/templates/partials/settings_saved_answers.html.j2
  - app/web/templates/partials/settings_playwright.html.j2
  - app/web/routers/settings.py
  - tests/saved_answers/__init__.py
  - tests/saved_answers/test_saved_answers_router.py
autonomous: true

must_haves:
  truths:
    - "User can view all saved question->answer pairs in a Settings section"
    - "User can edit or delete any saved answer"
    - "User can toggle headless vs headed mode in Settings"
    - "User can toggle pause_if_unsure vs always_advance in Settings"
    - "User can set screenshot retention days in Settings"
  artifacts:
    - path: "app/web/routers/saved_answers.py"
      provides: "Saved Answers CRUD router"
      contains: "router"
    - path: "app/web/templates/partials/settings_saved_answers.html.j2"
      provides: "Saved Answers settings section partial"
      contains: "saved_answer"
    - path: "app/web/templates/partials/settings_playwright.html.j2"
      provides: "Playwright settings section partial"
      contains: "playwright_headless"
  key_links:
    - from: "app/web/routers/saved_answers.py"
      to: "app/learning/service.py"
      via: "CRUD operations"
      pattern: "get_all_saved_answers|update_saved_answer|delete_saved_answer"
    - from: "app/web/routers/settings.py"
      to: "app/web/templates/partials/settings_playwright.html.j2"
      via: "Settings section loading"
      pattern: "playwright"
---

<objective>
Build the Saved Answers management section and Playwright configuration toggles in the Settings UI.

Purpose: This delivers LEARN-03 (answers persisted and reusable) visible in a management UI, plus the locked-decision Settings toggles for headless mode, pause_if_unsure, and screenshot retention. The Saved Answers section is the user's view into the learning loop's accumulated knowledge.
Output: Saved Answers CRUD router + 2 new settings partials + settings router updates.
</objective>

<execution_context>
@C:\Users\abuba\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\abuba\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-playwright-browser-submission-learning-loop/06-RESEARCH.md
@.planning/phases/06-playwright-browser-submission-learning-loop/06-01-PLAN.md
@.planning/phases/06-playwright-browser-submission-learning-loop/06-04-PLAN.md
@app/web/routers/settings.py
@app/web/templates/settings.html.j2
@app/web/templates/partials/settings_submission.html.j2
@app/web/templates/partials/settings_sidebar.html.j2
</context>

<tasks>

<task type="auto">
  <name>Task 1: Saved Answers CRUD router + settings partial</name>
  <files>app/web/routers/saved_answers.py, app/web/templates/partials/settings_saved_answers.html.j2, app/web/routers/settings.py, app/main.py, tests/saved_answers/__init__.py, tests/saved_answers/test_saved_answers_router.py</files>
  <action>
  **app/web/routers/saved_answers.py** — APIRouter(prefix="/settings/saved-answers"):

  **GET /settings/saved-answers** — Render the saved answers section
  - Call `get_all_saved_answers(session)` from `app.learning.service`
  - Render `partials/settings_saved_answers.html.j2` with the list of saved answers
  - Each answer shows: field_label, answer_text, answer_type, times_reused, source job (if available), created_at
  - Edit and Delete buttons per row

  **POST /settings/saved-answers/{answer_id}/edit** — Update answer text
  - Parse form: `answer_text: str`
  - Call `update_saved_answer(session, answer_id, answer_text)`
  - Return re-rendered partial (HTMX swap pattern matching existing settings sections)

  **POST /settings/saved-answers/{answer_id}/delete** — Delete saved answer
  - Call `delete_saved_answer(session, answer_id)`
  - Return re-rendered partial (answer removed from list)

  **Template: settings_saved_answers.html.j2:**
  - Table with columns: Question (field_label), Answer (answer_text), Type, Times Reused, Actions (Edit/Delete)
  - Edit inline: clicking Edit makes the answer_text cell editable (simple pattern — replace text with input, submit via HTMX POST)
  - Delete: confirm via browser confirm() then HTMX DELETE
  - Empty state: "No saved answers yet. Answers are created when you respond to unknown form fields."
  - Style: match existing Keywords management page aesthetic (simple list with edit/delete actions per locked decision)

  **Wire into settings sidebar:**
  - In `app/web/routers/settings.py`, add the "saved-answers" section to the sidebar routing (same pattern as existing sections like keywords, credentials, etc.)
  - Add a `_render_saved_answers` helper or similar routing entry
  - Position "Saved Answers" in the sidebar AFTER "Submission" and BEFORE "Safety" (or at the end of the Phase 6 group)

  **app/main.py:**
  - Import and mount `saved_answers_router` if it's a separate router module (or confirm it's loaded via the settings router)

  Tests:
  - test_saved_answers_list: create SavedAnswer rows, GET returns 200 with answer data
  - test_saved_answers_empty: no answers -> 200 with empty state message
  - test_saved_answers_edit: POST edit -> answer_text updated, re-rendered partial returned
  - test_saved_answers_delete: POST delete -> answer removed, re-rendered partial returned
  - test_saved_answers_edit_404: invalid answer_id -> 404
  </action>
  <verify>Run `python -m pytest tests/saved_answers/test_saved_answers_router.py -v`</verify>
  <done>Saved Answers section in Settings shows all question->answer pairs with edit/delete. CRUD operations work via HTMX. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright settings toggles (headless, pause_if_unsure, retention)</name>
  <files>app/web/templates/partials/settings_playwright.html.j2, app/web/routers/settings.py, tests/saved_answers/test_saved_answers_router.py</files>
  <action>
  **Template: settings_playwright.html.j2:**
  Create a Playwright settings section partial with three controls:

  1. **Headless Mode** toggle (checkbox):
     - Label: "Headless Browser Mode"
     - Description: "Run Playwright in headless mode (default). Disable to watch forms being filled in real time."
     - Maps to Settings.playwright_headless (default: True / checked)

  2. **Pause If Unsure** toggle (checkbox):
     - Label: "Pause on Unknown Fields"
     - Description: "Halt applications when form fields can't be auto-filled. Disable for 'always advance' mode (screenshots captured but no halts)."
     - Maps to Settings.pause_if_unsure (default: True / checked)

  3. **Screenshot Retention** number input:
     - Label: "Screenshot Retention (days)"
     - Description: "Screenshots older than this are automatically deleted. Screenshots for halted applications are kept until resolved."
     - Maps to Settings.screenshot_retention_days (default: 30)
     - Min: 1, Max: 365

  Style matches existing settings section partials (same form layout, same button styles).

  **app/web/routers/settings.py:**
  - Add GET handler for the playwright section (same pattern as other settings sections — load Settings row, render partial)
  - Add POST handler for saving playwright settings:
    - Parse form: playwright_headless (checkbox — missing = False), pause_if_unsure (checkbox), screenshot_retention_days (int, clamp to 1-365)
    - Update Settings row
    - Return re-rendered partial with success indicator
  - Add "Playwright" to the sidebar section list, positioned after "Submission" alongside "Saved Answers"

  **Update settings_sidebar.html.j2** (if it's a standalone partial that lists sections):
  - Add "Playwright" and "Saved Answers" entries

  Add tests:
  - test_playwright_settings_get: GET returns 200 with current values
  - test_playwright_settings_post: POST updates all three fields correctly
  - test_playwright_settings_checkbox_unchecked: unchecked headless = False stored
  - test_playwright_settings_retention_clamped: values outside 1-365 clamped

  Run full suite to verify no regressions.
  </action>
  <verify>Run `python -m pytest tests/ -x -q`</verify>
  <done>Playwright settings section shows headless toggle, pause_if_unsure toggle, and screenshot retention input. Settings persist correctly. Sidebar lists both new sections. Full suite green.</done>
</task>

</tasks>

<verification>
- GET /settings/saved-answers returns 200 with saved answers list
- Edit/Delete operations work on saved answers
- GET /settings/playwright returns 200 with current toggle values
- POST /settings/playwright persists headless, pause_if_unsure, retention_days
- Sidebar includes "Playwright" and "Saved Answers" sections
- Full test suite passes
</verification>

<success_criteria>
User can view, edit, and delete saved answers in Settings. User can toggle headless/headed mode, pause_if_unsure/always_advance, and set screenshot retention. Settings persist across page loads. All tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/06-playwright-browser-submission-learning-loop/06-07-SUMMARY.md`
</output>
