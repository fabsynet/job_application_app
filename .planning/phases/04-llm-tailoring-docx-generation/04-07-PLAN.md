---
phase: 04-llm-tailoring-docx-generation
plan: 07
type: execute
wave: 5
depends_on: ["04-05", "04-06"]
files_modified:
  - tests/test_phase4_tailoring.py
  - tests/conftest.py
autonomous: true

must_haves:
  truths:
    - "Hallucination validator rejects invented skill in test case"
    - "PII never appears in LLM prompt (SAFE-04 test)"
    - "Budget halt stops tailoring at 100% cap"
    - "DOCX writer preserves run-level formatting"
    - "Versioned artifacts stored and queryable"
    - "Settings intensity persists correctly"
    - "ATS checks detect tables and non-standard fonts"
    - "Cache read tokens and savings displayed in tailoring detail template"
  artifacts:
    - path: "tests/test_phase4_tailoring.py"
      provides: "Integration and unit tests for all TAIL requirements"
      min_lines: 200
  key_links:
    - from: "tests/test_phase4_tailoring.py"
      to: "app/tailoring/"
      via: "imports from all tailoring submodules"
      pattern: "from app.tailoring"
---

<objective>
Write comprehensive tests covering all Phase 4 requirements: TAIL-01 through TAIL-09 and SAFE-04.

Purpose: The phase success criteria require testable verification: hallucination validator rejecting invented skills (SC-2), PII not in LLM prompts (SC-3), budget halt (SC-4), versioned artifacts (SC-5). These tests prove the phase works without requiring live Claude API calls.

Output: Test file covering all 10 requirements with mocked LLM calls and real DOCX operations.
</objective>

<execution_context>
@C:\Users\abuba\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\abuba\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-llm-tailoring-docx-generation/04-RESEARCH.md
@app/tailoring/engine.py
@app/tailoring/provider.py
@app/tailoring/budget.py
@app/tailoring/docx_writer.py
@app/tailoring/preview.py
@app/tailoring/service.py
@app/tailoring/pipeline.py
@app/tailoring/prompts.py
@app/tailoring/models.py
@app/web/templates/partials/tailoring_detail.html.j2
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit tests for engine, budget, DOCX writer, and prompts</name>
  <files>tests/test_phase4_tailoring.py, tests/conftest.py</files>
  <action>
Create `tests/test_phase4_tailoring.py` with the following test groups. Use pytest + pytest-asyncio. Mock LLM calls with a FakeLLMProvider that returns canned responses.

**Test fixtures (add to conftest.py or local fixtures):**

```python
class FakeLLMProvider:
    """Mock LLM provider returning canned responses."""
    def __init__(self, responses: list[str]):
        self._responses = iter(responses)
        self.calls = []
    
    async def complete(self, system, messages, max_tokens, temperature=0.3):
        self.calls.append({"system": system, "messages": messages})
        content = next(self._responses)
        return LLMResponse(content=content, input_tokens=100, output_tokens=50,
                          cache_creation_tokens=0, cache_read_tokens=80, model="fake")
```

Create a small test DOCX fixture: a real .docx file with headings and bullets created via python-docx in a fixture (`tmp_path`). Include: a "Contact" section (name, email, phone), "Professional Summary", "Work Experience" with company/title/dates/bullets, "Skills" section.

**Test group: PII stripping (SAFE-04)**
- `test_strip_pii_removes_contact_section`: Pass sections with a None-heading contact block. Verify the output text contains no name/email/phone.
- `test_strip_pii_preserves_content_sections`: Verify work experience and skills sections survive stripping.
- `test_strip_pii_logs_warning_when_no_contact_stripped`: If all sections have headings, verify warning is logged.

**Test group: Prompts**
- `test_system_messages_have_cache_control`: Verify build_system_messages returns a list where the second item has cache_control.
- `test_intensity_instructions_differ`: Verify build_tailoring_user_message produces different text for light/balanced/full.
- `test_escalation_suffix_increases`: Verify get_escalated_prompt_suffix returns progressively stricter text for retry 0, 1, 2.

**Test group: Hallucination validator (TAIL-04)**
- `test_validator_passes_clean_output`: FakeLLMProvider returns `{"passed": true, "violations": []}`. Verify validate_output returns (True, []).
- `test_validator_rejects_invented_skill`: FakeLLMProvider returns `{"passed": false, "violations": [{"type": "invented_skill", "content": "React", "explanation": "Not in original"}]}`. Verify validate_output returns (False, [...]).
- **KEY TEST (Success Criteria 2):** `test_invented_skill_rejected_end_to_end`: Create a FakeLLMProvider that returns tailored JSON containing "React" (not in base resume), then a validator response rejecting it. Verify tailor_resume returns success=False after retries, with validation_warnings containing the invented skill.

**Test group: Budget guard (TAIL-08)**
- `test_budget_check_allows_under_cap`: Set budget_cap=10.0, spent=5.0. Verify can_proceed=True.
- `test_budget_check_halts_at_cap`: Set budget_cap=10.0, spent=10.0. Verify can_proceed=False.
- `test_budget_warning_at_80_percent`: Set budget_cap=10.0, spent=8.0. Verify is_warning=True.
- `test_budget_month_rollover`: Set budget_month to last month, spent=50.0. Verify check resets spent to 0.
- `test_budget_no_cap_always_proceeds`: Set budget_cap=0. Verify can_proceed=True regardless of spent.
- `test_estimate_cost_calculation`: Verify estimate_cost returns expected value for known token counts.

**Test group: DOCX writer (TAIL-05)**
- `test_build_tailored_docx_preserves_formatting`: Create a base DOCX with bold/italic runs. Run build_tailored_docx with replacement text. Open output and verify bold/italic runs still exist.
- `test_build_tailored_docx_replaces_content`: Verify the tailored text appears in the output DOCX.
- `test_cover_letter_docx_created`: Verify build_cover_letter_docx creates a valid DOCX with the expected paragraphs.
- `test_ats_check_detects_table`: Create a DOCX with a table. Verify check_ats_friendly reports has_tables=True.
- `test_ats_check_detects_nonstandard_font`: Create a DOCX with "Comic Sans MS". Verify it appears in non_standard_fonts.
- `test_keyword_coverage_computation`: Verify compute_keyword_coverage returns expected ratio.

**Test group: Preview**
- `test_docx_to_html_returns_string`: Convert test DOCX to HTML, verify non-empty string returned.
- `test_section_diff_identifies_changes`: Provide base and tailored sections with one changed section. Verify diff output has changed=True for that section.

**Test group: Service layer (TAIL-09)**
- `test_artifact_paths_versioned`: Verify resume_artifact_path(42, 1) returns path containing "42/v1.docx".
- `test_save_and_query_tailoring_record`: Save a record, query it back, verify all fields.

**Test group: Settings UI**
- `test_settings_tailoring_intensity_persists`: POST to /settings/tailoring with intensity="full". GET settings, verify tailoring_intensity="full".

**Test group: Cache token visibility (SC-5)**
- `test_tailoring_detail_template_shows_cache_tokens`: Read the `tailoring_detail.html.j2` template file and assert it contains `cache_read_tokens` (or equivalent variable name) and "cache savings" (or "estimated savings") text. This verifies the template surfaces prompt caching cost reduction to the user.
- `test_tailoring_result_tracks_cache_tokens`: Create a TailoringResult with total_cache_read_tokens=500. Verify the field is accessible and non-zero, confirming the data flows from engine to UI.

All DB tests should use the existing test session fixture pattern from conftest.py. All DOCX tests should use tmp_path for file operations.
  </action>
  <verify>
`python -m pytest tests/test_phase4_tailoring.py -x -v` -- all tests pass. `python -m pytest tests/ -x -q` -- full suite passes (no regressions).
  </verify>
  <done>Comprehensive test coverage for all TAIL requirements and SAFE-04. Key tests: invented skill rejected (SC-2), PII not in prompts (SC-3), budget halt at cap (SC-4), formatting preserved (SC-1), versioned artifacts (SC-5), cache token visibility (SC-5). All tests use mocked LLM calls.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_phase4_tailoring.py -v` shows all tests passing
- `python -m pytest tests/ -x -q` shows no regressions
- Test count increases by 20+ tests
- Each TAIL requirement has at least one dedicated test
- SAFE-04 has explicit PII-in-prompt assertion test
- SC-2 hallucination test explicitly injects an invented skill and verifies rejection
- SC-5 cache token visibility has dedicated template assertion test
</verification>

<success_criteria>
- All Phase 4 requirements (TAIL-01 through TAIL-09, SAFE-04) have test coverage
- Hallucination validator test case injects invented skill and verifies rejection (SC-2)
- PII assertion test proves name/email/phone never appear in LLM prompt text (SC-3)
- Budget halt test proves tailoring stops at 100% cap (SC-4)
- DOCX formatting preservation tested with bold/italic verification (SC-1)
- Cache token visibility tested via template assertion (SC-5)
- No regressions in existing 175 tests
</success_criteria>

<output>
After completion, create `.planning/phases/04-llm-tailoring-docx-generation/04-07-SUMMARY.md`
</output>
