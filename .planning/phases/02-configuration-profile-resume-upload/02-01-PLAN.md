---
phase: 02-configuration-profile-resume-upload
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/db/models.py
  - app/db/migrations/versions/0002_phase2_config.py
  - app/web/routers/settings.py
  - app/web/templates/settings.html.j2
  - app/web/templates/partials/settings_sidebar.html.j2
  - app/web/templates/partials/settings_mode.html.j2
  - app/web/templates/partials/settings_limits.html.j2
  - app/web/templates/partials/settings_safety.html.j2
  - app/web/templates/partials/flash_message.html.j2
  - app/web/static/app.css
  - requirements.txt
autonomous: true

must_haves:
  truths:
    - "Settings page renders a left sidebar with section links and a right content area"
    - "Clicking a sidebar link loads that section partial into the content area via HTMX"
    - "Phase 1 settings (rate limits, kill-switch, dry-run) are accessible via the new sidebar"
    - "Mode toggle (full-auto vs review-queue) appears as the first sidebar section"
    - "Database has all Phase 2 columns and Profile table after migration"
  artifacts:
    - path: "app/db/migrations/versions/0002_phase2_config.py"
      provides: "Alembic migration adding Settings columns + Profile table"
      contains: "match_threshold"
    - path: "app/web/templates/settings.html.j2"
      provides: "Sidebar layout shell"
      contains: "settings-layout"
    - path: "app/web/templates/partials/settings_mode.html.j2"
      provides: "Mode toggle section partial"
      contains: "auto_mode"
  key_links:
    - from: "app/web/routers/settings.py"
      to: "app/web/templates/partials/"
      via: "HTMX hx-get section endpoints"
      pattern: "/settings/section/"
    - from: "app/db/models.py"
      to: "app/db/migrations/versions/0002_phase2_config.py"
      via: "Alembic migration adds columns matching model"
      pattern: "add_column.*settings"
---

<objective>
Create the database foundation (migration + Profile model + new Settings columns), add python-docx dependency, and build the sidebar layout shell that all subsequent plans will render their section partials into. Merge Phase 1 settings (rate limits, kill-switch, dry-run) into the sidebar. Implement the Mode toggle section as the first functional section.

Purpose: Every other plan in Phase 2 depends on the sidebar shell existing and the database having the right columns. This plan removes that bottleneck.
Output: Working sidebar-navigated settings page with Mode toggle functional, Phase 1 settings migrated into sidebar sections, database ready for all Phase 2 data.
</objective>

<execution_context>
@C:\Users\abuba\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\abuba\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-configuration-profile-resume-upload/02-RESEARCH.md
@app/db/models.py
@app/settings/service.py
@app/web/routers/settings.py
@app/web/templates/settings.html.j2
@app/web/static/app.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database migration + Profile model + python-docx dependency</name>
  <files>
    app/db/models.py
    app/db/migrations/versions/0002_phase2_config.py
    requirements.txt
  </files>
  <action>
1. Add `python-docx==1.2.0` to requirements.txt.

2. Extend the Settings model in app/db/models.py with these new fields:
   - match_threshold: int = Field(default=60)
   - schedule_enabled: bool = Field(default=False)
   - quiet_hours_start: int = Field(default=22)  # 0-23
   - quiet_hours_end: int = Field(default=7)     # 0-23
   - budget_cap_dollars: float = Field(default=0.0)  # 0 = no cap
   - budget_spent_dollars: float = Field(default=0.0)
   - budget_month: str = Field(default="")  # e.g. "2026-04"
   - auto_mode: bool = Field(default=True)  # True=full-auto, False=review-queue
   - resume_filename: Optional[str] = Field(default=None)
   - resume_uploaded_at: Optional[datetime] = Field(default=None)

3. Add a new Profile model (separate table):
   - id: int (primary_key, default=1, singleton like Settings)
   - full_name: Optional[str]
   - email: Optional[str]
   - phone: Optional[str]
   - address: Optional[str]
   - work_authorization: Optional[str]
   - salary_expectation: Optional[str]
   - years_experience: Optional[int]
   - linkedin_url: Optional[str]
   - github_url: Optional[str]
   - portfolio_url: Optional[str]
   - updated_at: datetime (default_factory=datetime.utcnow)

4. Create Alembic migration 0002_phase2_config.py:
   - Use op.add_column for each new Settings column with server_default (SQLite requirement!)
   - Use op.create_table for profile table
   - All new Settings columns must have server_default values
   - Pattern: sa.Column("match_threshold", sa.Integer(), server_default=sa.text("60"), nullable=False)
   - For resume_filename and resume_uploaded_at: nullable=True, no server_default needed
   - For budget_month: server_default=sa.text("''")

5. Also update keywords_csv to use pipe delimiter in the research recommendation. Actually, keep the column name as-is but the service layer will use pipe `|` as delimiter going forward. Add a comment on the field noting pipe delimiter.
  </action>
  <verify>
Run `cd /c/Users/abuba/Downloads/cla_project/job_application_app && python -c "from app.db.models import Settings, Profile; print('Models OK')"` and verify no import errors. Run `alembic upgrade head` in the app directory (or verify migration file is syntactically valid with `python -c "import app.db.migrations.versions.0002_phase2_config"`).
  </verify>
  <done>Settings model has all Phase 2 columns, Profile model exists, migration file is valid, python-docx in requirements.txt.</done>
</task>

<task type="auto">
  <name>Task 2: Sidebar layout shell + section endpoints + merge Phase 1 settings</name>
  <files>
    app/web/routers/settings.py
    app/web/templates/settings.html.j2
    app/web/templates/partials/settings_sidebar.html.j2
    app/web/templates/partials/settings_mode.html.j2
    app/web/templates/partials/settings_limits.html.j2
    app/web/templates/partials/settings_safety.html.j2
    app/web/templates/partials/flash_message.html.j2
    app/web/static/app.css
  </files>
  <action>
1. Rewrite app/web/templates/settings.html.j2 to use a sidebar layout:
   - Flexbox layout: aside.settings-sidebar (220px fixed) + div#settings-content (flex:1)
   - Sidebar contains nav ul with links for each section, each with hx-get="/settings/section/{name}" hx-target="#settings-content"
   - Section order (per research recommendation): Mode, Profile, Resume, Keywords, Threshold, Credentials, Schedule, Budget, Rate Limits, Safety
   - Default content area loads the Mode section (first/most consequential)
   - Active sidebar link gets class="active" — use HTMX hx-on::after-request or server-side `active_section` variable

2. Create app/web/templates/partials/flash_message.html.j2:
   - Accepts flash tuple (type, message): renders green-left-border for success, red for error
   - Auto-dismiss after 3 seconds via a small inline script using setTimeout to remove the element

3. Create app/web/templates/partials/settings_mode.html.j2:
   - Radio buttons or toggle for auto_mode (Full Auto / Review Queue)
   - Clear labels explaining each mode ("Applications submitted automatically" vs "Applications queued for your approval")
   - Save button, form POSTs to /settings/mode via HTMX
   - Include flash_message partial if flash is set

4. Create app/web/templates/partials/settings_limits.html.j2:
   - Move the existing rate-limits form from the old settings.html.j2 into this partial
   - Fields: daily_cap, delay_min_seconds, delay_max_seconds, timezone
   - Save button, form POSTs to /settings/limits via HTMX (refactor existing POST /settings/limits to return partial instead of redirect)

5. Create app/web/templates/partials/settings_safety.html.j2:
   - Kill-switch toggle and dry-run toggle (these were on the dashboard in Phase 1 via /toggles but should also be accessible here)
   - Form POSTs to /settings/safety via HTMX, returns partial with flash

6. Refactor app/web/routers/settings.py:
   - Keep existing /settings GET but load Settings row and render new sidebar layout with default section=mode
   - Add GET /settings/section/{section_name} — returns the appropriate partial template
   - Add POST /settings/mode — saves auto_mode, returns mode partial with flash
   - Refactor POST /settings/limits to return the limits partial (with flash) instead of RedirectResponse
   - Add POST /settings/safety — saves kill_switch and dry_run, returns safety partial with flash
   - Keep existing secrets endpoints for now (will be refactored into credentials section in plan 04)
   - Helper function _render_section(request, session, section_name, flash=None) that builds context and renders the right partial

7. Add sidebar + flash CSS to app/web/static/app.css:
   - .settings-layout { display: flex; gap: 2rem; min-height: 60vh; }
   - .settings-sidebar { width: 220px; flex-shrink: 0; border-right: 1px solid var(--pico-muted-border-color); padding-right: 1rem; }
   - .settings-sidebar nav ul { list-style: none; padding: 0; }
   - .settings-sidebar nav a { display: block; padding: 0.5rem 0.75rem; border-radius: 0.25rem; text-decoration: none; }
   - .settings-sidebar nav a.active { background: var(--pico-primary-background); color: var(--pico-primary-inverse); }
   - .settings-content { flex: 1; min-width: 0; }
   - .flash-success { border-left: 4px solid #2a9d8f; background: #f0fff4; padding: 0.75rem 1rem; margin: 1rem 0; }
   - .flash-error { border-left: 4px solid #e76f51; background: #fff3f0; padding: 0.75rem 1rem; margin: 1rem 0; }

8. For sections not yet implemented (Profile, Resume, Keywords, Threshold, Credentials, Schedule, Budget), the GET /settings/section/{name} endpoint should return a simple placeholder partial saying "Coming soon" so the sidebar is fully navigable but non-functional sections don't crash.

IMPORTANT: The existing secrets CRUD routes (POST /settings/secrets, DELETE /settings/secrets/{name}) must remain functional — plan 04 will refactor them into the credentials section.
  </action>
  <verify>
Run the test suite: `pytest tests/ -x -q`. Start the app and verify GET /settings returns a page with sidebar navigation. Verify GET /settings/section/mode returns the mode toggle partial. Verify POST /settings/mode with auto_mode=true saves and returns flash. Verify POST /settings/limits returns a partial (not a redirect). Verify clicking sidebar links loads different sections.
  </verify>
  <done>Settings page renders sidebar layout with all section links. Mode toggle saves successfully. Rate limits and safety sections work as partials. Placeholder content shows for sections built in later plans. Existing secrets endpoints still work. All existing tests pass (with adaptations for the refactored limits endpoint if needed).</done>
</task>

</tasks>

<verification>
- `pytest tests/ -x -q` passes (existing tests may need minor updates for limits endpoint redirect->partial change)
- GET /settings shows sidebar with 10 section links
- GET /settings/section/mode shows mode toggle form
- POST /settings/mode saves auto_mode field
- GET /settings/section/limits shows rate limit form
- GET /settings/section/safety shows kill-switch + dry-run toggles
- Profile model importable from app.db.models
- Migration file exists and is syntactically valid
</verification>

<success_criteria>
- Sidebar-navigated settings page is the single entry point for all configuration
- Mode toggle (full-auto vs review-queue) is functional end-to-end
- Phase 1 settings (limits, safety toggles) work within the new sidebar layout
- Database schema supports all Phase 2 data (profile, resume metadata, keywords, threshold, budget, schedule, mode)
- python-docx dependency added to requirements.txt
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-configuration-profile-resume-upload/02-01-SUMMARY.md`
</output>
