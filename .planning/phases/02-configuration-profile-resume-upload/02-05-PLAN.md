---
phase: 02-configuration-profile-resume-upload
plan: 05
type: execute
wave: 3
depends_on: ["02-02", "02-03", "02-04"]
files_modified:
  - tests/test_phase2_settings.py
  - tests/test_phase2_resume.py
  - tests/test_phase2_credentials.py
autonomous: true

must_haves:
  truths:
    - "All 8 CONF requirements are covered by automated tests"
    - "Test suite passes end-to-end proving each success criterion"
    - "Resume upload persists to filesystem and metadata to DB"
    - "Credential validation is tested (mocked network calls)"
    - "All settings sections save and load correctly"
  artifacts:
    - path: "tests/test_phase2_settings.py"
      provides: "Tests for keywords, threshold, schedule, budget, mode, sidebar navigation"
      min_lines: 80
    - path: "tests/test_phase2_resume.py"
      provides: "Tests for DOCX upload, extraction, persistence"
      min_lines: 40
    - path: "tests/test_phase2_credentials.py"
      provides: "Tests for credential save + validation (mocked)"
      min_lines: 50
  key_links:
    - from: "tests/test_phase2_settings.py"
      to: "app/web/routers/settings.py"
      via: "httpx TestClient hitting all section endpoints"
      pattern: "/settings/section/|/settings/mode|/settings/threshold"
    - from: "tests/test_phase2_credentials.py"
      to: "app/credentials/validation.py"
      via: "Mocked validation functions"
      pattern: "mock.*validate_anthropic|mock.*validate_smtp"
---

<objective>
Write integration tests covering all Phase 2 requirements (CONF-01 through CONF-08). Tests exercise the full stack: HTTP endpoint -> service layer -> database/filesystem, with mocked external network calls for credential validation.

Purpose: Goal-backward verification — each success criterion from the roadmap gets at least one test proving it works. Catches regressions and confirms the phase is complete.
Output: Green test suite proving all 8 CONF requirements are satisfied.
</objective>

<execution_context>
@C:\Users\abuba\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\abuba\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-configuration-profile-resume-upload/02-RESEARCH.md
@app/db/models.py
@app/web/routers/settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings integration tests (mode, keywords, threshold, schedule, budget)</name>
  <files>
    tests/test_phase2_settings.py
  </files>
  <action>
Create tests/test_phase2_settings.py with integration tests using httpx.AsyncClient against the FastAPI app (same pattern as Phase 1 tests). Use tmp_path for data dirs and reload app modules as established in Phase 1.

Tests to write:

1. test_sidebar_renders_all_sections — GET /settings returns HTML containing all sidebar section links
2. test_section_navigation — GET /settings/section/{each_name} returns 200 for all 10 sections
3. test_mode_toggle_save_auto — POST /settings/mode with auto_mode=true returns partial with flash success, verify DB
4. test_mode_toggle_save_review — POST /settings/mode with auto_mode=false, verify Settings.auto_mode is False
5. test_keywords_add — POST /settings/keywords with keyword="python", verify chip appears in response, verify DB has "python" in keywords_csv
6. test_keywords_add_dedup — Add same keyword twice, verify only one chip
7. test_keywords_remove — Add keyword, then DELETE /settings/keywords/python, verify removed from response and DB
8. test_keywords_empty_rejected — POST with empty keyword, verify error flash
9. test_threshold_save — POST /settings/threshold with match_threshold=75, verify DB, verify response shows "75%"
10. test_threshold_validation — POST with match_threshold=150, verify error (out of range)
11. test_schedule_save — POST /settings/schedule with schedule_enabled=on, quiet_hours_start=22, quiet_hours_end=7, verify DB
12. test_schedule_disable — POST without schedule_enabled field, verify Settings.schedule_enabled is False
13. test_budget_save — POST /settings/budget with budget_cap_dollars=20.0, verify DB
14. test_budget_zero_means_no_limit — POST with budget_cap_dollars=0, verify saves and response shows "No limit set"

Use `@pytest.mark.asyncio` and the established fixture pattern from Phase 1 tests (tmp_path data dir, reload modules, async session).
  </action>
  <verify>Run `pytest tests/test_phase2_settings.py -v` — all tests pass green.</verify>
  <done>14+ tests covering mode, keywords, threshold, schedule, budget sections. All pass. Cover CONF-02, CONF-03, CONF-04, CONF-06, CONF-08 requirements.</done>
</task>

<task type="auto">
  <name>Task 2: Resume upload + credential validation tests</name>
  <files>
    tests/test_phase2_resume.py
    tests/test_phase2_credentials.py
  </files>
  <action>
Create tests/test_phase2_resume.py:

1. test_resume_upload_docx — Create a minimal DOCX file in tmp_path (use python-docx to create a 2-paragraph doc), POST to /settings/resume as multipart upload, verify:
   - Response 200 with success flash
   - File exists at data/resumes/base_resume.docx
   - Settings.resume_filename is set
   - Settings.resume_uploaded_at is set
   - Response contains extracted text from the DOCX
2. test_resume_upload_non_docx_rejected — Upload a .txt file, verify error flash, no file saved
3. test_resume_replace — Upload one DOCX, then upload another, verify only one file exists and metadata updated
4. test_resume_preview_content — Upload a DOCX with headings + body text, verify preview shows heading text and body text
5. test_resume_section_no_upload — GET /settings/section/resume when no file exists, verify shows upload form only

Create tests/test_phase2_credentials.py:

1. test_anthropic_key_save_valid — Mock validate_anthropic_key to return (True, "valid"), POST /settings/credentials/anthropic with api_key="sk-test", verify:
   - Secret "anthropic_api_key" exists in DB (encrypted)
   - Response contains "valid" flash
2. test_anthropic_key_save_invalid — Mock validation to return (False, "Invalid"), verify key is still saved, flash shows "Invalid"
3. test_anthropic_key_save_network_error — Mock validation to return (False, "timed out"), verify key saved, flash warns
4. test_smtp_save_valid — Mock validate_smtp_credentials to return (True, "valid"), POST all 4 fields, verify all 4 secrets exist encrypted
5. test_smtp_save_invalid — Mock validation to return (False, "Auth failed"), verify secrets saved anyway, flash shows failure
6. test_credentials_status_configured — Save a key, then GET /settings/section/credentials, verify "Configured" appears
7. test_credentials_status_not_set — GET credentials section without saving anything, verify "Not set" appears
8. test_credentials_never_revealed — Save a key, GET credentials section, verify the actual key value does NOT appear in the response HTML

Mock validation functions using unittest.mock.patch or pytest-mock (whatever is established in the project). Mock at the router level: `app.web.routers.settings.validate_anthropic_key`.
  </action>
  <verify>Run `pytest tests/test_phase2_resume.py tests/test_phase2_credentials.py -v` — all tests pass green.</verify>
  <done>13+ tests covering resume upload/replace/preview/rejection and credential save/validation/status. All pass. Cover CONF-01, CONF-05, CONF-07 requirements.</done>
</task>

<task type="auto">
  <name>Task 3: Profile tests + full test suite verification</name>
  <files>
    tests/test_phase2_settings.py
  </files>
  <action>
Add profile tests to tests/test_phase2_settings.py (or a separate test_phase2_profile.py if the file is getting large):

1. test_profile_save — POST /settings/profile with full_name="John Doe", email="john@example.com", verify Profile row created with correct values
2. test_profile_all_optional — POST /settings/profile with all empty/missing fields, verify succeeds with no error
3. test_profile_edit — Save profile, then POST again with updated full_name, verify update persisted
4. test_profile_renders_existing — Save profile, then GET /settings/section/profile, verify form is pre-filled with saved values

Then run the FULL test suite (all Phase 1 + Phase 2 tests):
- `pytest tests/ -v --tb=short`
- Fix any regressions from Phase 1 tests (likely: tests that hit GET /settings and expect the old page structure will need updating to account for the new sidebar layout)
- The Phase 1 settings test for POST /settings/limits may need updating if the endpoint now returns a partial instead of a 303 redirect

Adapt failing Phase 1 tests:
- If a test checks for RedirectResponse from /settings/limits, update to check for 200 + partial HTML
- If a test checks for specific HTML structure in /settings, update assertions for sidebar layout
  </action>
  <verify>Run `pytest tests/ -v --tb=short` — ALL tests pass (Phase 1 + Phase 2). Zero failures.</verify>
  <done>Profile tests pass. Full test suite (Phase 1 + Phase 2) passes with zero regressions. All 8 CONF requirements have test coverage.</done>
</task>

</tasks>

<verification>
- `pytest tests/ -v` — all tests pass (Phase 1 + Phase 2)
- Each CONF requirement has at least one test:
  - CONF-01: resume upload + persistence tests
  - CONF-02: keywords add/remove/dedup tests
  - CONF-03: threshold slider save/validation tests
  - CONF-04: schedule enable/disable + quiet hours tests
  - CONF-05: profile save/edit/optional tests
  - CONF-06: mode toggle auto/review tests
  - CONF-07: credentials save/validate/status tests
  - CONF-08: budget cap save + progress bar tests
- No Phase 1 test regressions
</verification>

<success_criteria>
- 30+ new tests covering all Phase 2 functionality
- Full test suite (Phase 1 + Phase 2) passes green
- Each of the 5 phase success criteria is provably tested
- Credential validation is mocked (no real network calls in CI)
- Resume tests use real python-docx DOCX creation for realistic coverage
</success_criteria>

<output>
After completion, create `.planning/phases/02-configuration-profile-resume-upload/02-05-SUMMARY.md`
</output>
