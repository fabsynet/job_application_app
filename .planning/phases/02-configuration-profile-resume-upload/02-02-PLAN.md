---
phase: 02-configuration-profile-resume-upload
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/web/routers/settings.py
  - app/web/templates/partials/settings_profile.html.j2
  - app/web/templates/partials/settings_resume.html.j2
  - app/resume/__init__.py
  - app/resume/service.py
  - app/settings/service.py
autonomous: true

must_haves:
  truths:
    - "User can fill and save profile fields (name, email, phone, address, work auth, salary, experience, links) and see them persisted on reload"
    - "User can upload a DOCX resume via file picker or drag-and-drop and see extracted text preview"
    - "Resume file persists at /data/resumes/base_resume.docx across container restarts"
    - "User can replace an existing resume with a new upload"
    - "Profile fields are all optional — saving with blank fields succeeds"
  artifacts:
    - path: "app/web/templates/partials/settings_profile.html.j2"
      provides: "Profile form with three collapsible groups"
      contains: "full_name"
    - path: "app/web/templates/partials/settings_resume.html.j2"
      provides: "Resume upload form with preview"
      contains: "drop-zone"
    - path: "app/resume/service.py"
      provides: "DOCX upload, storage, text extraction"
      contains: "extract_resume_text"
  key_links:
    - from: "app/web/routers/settings.py"
      to: "app/resume/service.py"
      via: "save_resume + extract_resume_text calls"
      pattern: "save_resume|extract_resume_text"
    - from: "app/web/routers/settings.py"
      to: "app/db/models.py"
      via: "Profile CRUD and Settings resume metadata"
      pattern: "Profile|resume_filename"
---

<objective>
Implement the Profile section (three-group form with all application fields) and Resume Upload section (DOCX upload with drag-and-drop, file storage, and text preview extraction).

Purpose: These are the two data-heaviest input surfaces. Profile fields feed the submission pipeline. The base resume is the raw material for LLM tailoring.
Output: Fully functional profile editor and resume upload with preview, persisted to DB and filesystem respectively.
</objective>

<execution_context>
@C:\Users\abuba\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\abuba\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-configuration-profile-resume-upload/02-RESEARCH.md
@app/db/models.py
@app/settings/service.py
@app/web/routers/settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Profile section — form + CRUD</name>
  <files>
    app/web/templates/partials/settings_profile.html.j2
    app/web/routers/settings.py
    app/settings/service.py
  </files>
  <action>
1. Add profile helpers to app/settings/service.py:
   - `get_profile_row(session) -> Profile` — get-or-create singleton (id=1), same pattern as get_settings_row
   - `update_profile(session, **kwargs) -> Profile` — update any provided fields, bump updated_at

2. Add routes to app/web/routers/settings.py:
   - GET /settings/section/profile — loads Profile row, renders settings_profile.html.j2
   - POST /settings/profile — accepts all profile fields from Form(..., default=None), calls update_profile, returns profile partial with success flash
   - No validation gates — all fields optional per user decision. Only light format validation: if email is provided and non-empty, check it contains "@"; if phone provided, strip non-digits

3. Create app/web/templates/partials/settings_profile.html.j2:
   - Three visually distinct sub-sections (use `<details>` elements for collapsible, open by default):
     a) Contact: full_name, email, phone, address (textarea for multiline)
     b) Work Details: work_authorization (dropdown: "US Citizen", "Green Card", "H1B", "OPT/CPT", "Other"), salary_expectation (text input, e.g. "$80,000-$100,000"), years_experience (number input)
     c) Links: linkedin_url, github_url, portfolio_url
   - Single Save button at the bottom
   - Form uses hx-post="/settings/profile" hx-target="#settings-content" hx-swap="innerHTML"
   - Include flash_message partial at the top if flash is set
   - Pre-fill all fields from the profile object
  </action>
  <verify>
Start app, navigate to /settings, click Profile in sidebar. Fill in some fields, click Save, see success flash. Reload page — fields persist. Leave all fields blank, save — succeeds without error.
  </verify>
  <done>Profile form renders with three collapsible groups, saves all fields to Profile table, shows success flash, persists across reloads, all fields optional.</done>
</task>

<task type="auto">
  <name>Task 2: Resume upload — file storage + DOCX extraction + preview</name>
  <files>
    app/resume/__init__.py
    app/resume/service.py
    app/web/templates/partials/settings_resume.html.j2
    app/web/routers/settings.py
  </files>
  <action>
1. Create app/resume/__init__.py (empty).

2. Create app/resume/service.py with:
   - RESUME_DIR = Path(get_settings().data_dir) / "resumes" (or use a constant that resolves to /data/resumes in production)
   - `async def save_resume(file: UploadFile) -> Path`: saves to RESUME_DIR/base_resume.docx, creates dir if needed, uses shutil.copyfileobj
   - `def extract_resume_text(file_path: Path) -> dict`: uses python-docx Document() to iterate paragraphs, returns {"full_text": str, "sections": [{"heading": str|None, "text": str}]}. Sections split on Heading styles. Cap full_text at 500 lines.
   - `def get_resume_path() -> Path | None`: returns the path if file exists, None otherwise

3. Add routes to app/web/routers/settings.py:
   - GET /settings/section/resume — checks if resume exists (file + metadata in Settings), renders resume partial with preview if exists
   - POST /settings/resume — accepts UploadFile named "resume", validates it's a .docx (check filename extension and content-type), saves via resume service, updates Settings row (resume_filename, resume_uploaded_at), extracts text for preview, returns resume partial with flash

4. Create app/web/templates/partials/settings_resume.html.j2:
   - If no resume uploaded: show upload form only
   - Upload form: file input (accept=".docx") + submit button
   - Drag-and-drop zone: a div#drop-zone with "Drop your .docx file here" text
   - Small vanilla JS snippet (~15 lines) for drag-and-drop:
     * Listen for dragover (preventDefault, add highlight class), dragleave (remove highlight), drop (preventDefault, get file, set file input files, submit form)
   - Form must have hx-post="/settings/resume" hx-target="#settings-content" hx-swap="innerHTML" hx-encoding="multipart/form-data"
   - If resume exists: show filename, upload date (formatted nicely), and a "Replace" button (same upload form below)
   - Preview section: render extracted text with headings as bold/strong labels, body text below. Use a scrollable div (max-height: 400px, overflow-y: auto) for the preview content.
   - Include flash message if set

IMPORTANT: Use hx-encoding="multipart/form-data" on the form — HTMX does not auto-detect file inputs (pitfall from research).
IMPORTANT: Save to the data directory from get_settings().data_dir / "resumes" path, which maps to the host-mounted ./data volume for container persistence.
  </action>
  <verify>
Start app, navigate to /settings, click Resume in sidebar. Upload a .docx file — see success flash and text preview. Verify file exists at data/resumes/base_resume.docx. Upload a different file — old one is replaced. Try uploading a non-.docx file — see error flash. Check that resume_filename and resume_uploaded_at are set in the Settings row.
  </verify>
  <done>Resume upload works via file picker and drag-and-drop. DOCX text is extracted and shown as preview. File persists at /data/resumes/. Metadata (filename, date) stored in Settings. Replace works. Non-DOCX rejected with error flash.</done>
</task>

</tasks>

<verification>
- Profile section: form renders, saves, persists, all fields optional
- Resume section: upload via picker works, drag-and-drop works, preview shows extracted text
- Resume file stored at data/resumes/base_resume.docx (host volume)
- Settings row updated with resume_filename and resume_uploaded_at after upload
- Non-DOCX upload rejected gracefully
- All existing tests pass
</verification>

<success_criteria>
- User can fill and edit a full application profile with 10+ fields across three groups
- User can upload a DOCX resume and see its extracted content as a preview
- Resume persists on disk (container-restart safe via mounted volume)
- Profile data persists in the Profile database table
- Both sections integrate cleanly with the sidebar layout from plan 01
</success_criteria>

<output>
After completion, create `.planning/phases/02-configuration-profile-resume-upload/02-02-SUMMARY.md`
</output>
