---
phase: 02-configuration-profile-resume-upload
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/web/routers/settings.py
  - app/web/templates/partials/settings_keywords.html.j2
  - app/web/templates/partials/settings_threshold.html.j2
  - app/web/templates/partials/settings_schedule.html.j2
  - app/web/templates/partials/settings_budget.html.j2
  - app/web/static/app.css
autonomous: true

must_haves:
  truths:
    - "User can add keywords as chips (type + Enter) and remove them (click X)"
    - "User can set match threshold via slider and see percentage label (Loose/Moderate/Strict)"
    - "User can enable/disable schedule and set quiet hours start/end"
    - "User can set a monthly budget cap and see a usage progress bar"
    - "All settings persist across page reloads"
  artifacts:
    - path: "app/web/templates/partials/settings_keywords.html.j2"
      provides: "Chip/tag keyword management UI"
      contains: "chip"
    - path: "app/web/templates/partials/settings_threshold.html.j2"
      provides: "Slider with labels"
      contains: "range"
    - path: "app/web/templates/partials/settings_schedule.html.j2"
      provides: "Schedule toggle + quiet hours sliders"
      contains: "quiet_hours"
    - path: "app/web/templates/partials/settings_budget.html.j2"
      provides: "Budget cap input + progress bar"
      contains: "budget_cap"
  key_links:
    - from: "app/web/routers/settings.py"
      to: "app/settings/service.py"
      via: "set_setting calls for each control"
      pattern: "set_setting.*match_threshold|keywords|schedule|budget"
    - from: "app/web/templates/partials/settings_keywords.html.j2"
      to: "/settings/keywords"
      via: "hx-post and hx-delete for add/remove"
      pattern: "hx-post.*keywords|hx-delete.*keywords"
---

<objective>
Implement four settings sections: Keywords (chip/tag management), Match Threshold (slider with labels), Schedule & Quiet Hours (toggle + hour sliders), and Budget Cap (dollar input + progress bar).

Purpose: These are the pipeline control parameters. Keywords + threshold control what jobs get matched. Schedule controls when the pipeline runs. Budget controls how much LLM spend is allowed.
Output: Four fully functional settings sections with HTMX inline saves.
</objective>

<execution_context>
@C:\Users\abuba\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\abuba\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-configuration-profile-resume-upload/02-RESEARCH.md
@app/db/models.py
@app/settings/service.py
@app/web/routers/settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Keywords chip/tag management</name>
  <files>
    app/web/templates/partials/settings_keywords.html.j2
    app/web/routers/settings.py
    app/web/static/app.css
  </files>
  <action>
1. Add keyword management routes to settings router:
   - GET /settings/section/keywords — reads keywords from Settings.keywords_csv (pipe-delimited), splits into list, renders partial
   - POST /settings/keywords — accepts `keyword: str = Form(...)`, trims whitespace, deduplicates against existing list, appends to pipe-delimited string, saves via set_setting, returns keywords partial with flash
   - DELETE /settings/keywords/{keyword} — removes keyword from the pipe-delimited string, saves, returns keywords partial (no flash needed for removal, or subtle flash)
   - Handle edge cases: empty keyword (reject), duplicate (reject with flash), max ~50 keywords (soft warning)

2. Create app/web/templates/partials/settings_keywords.html.j2:
   - Container div#keywords-section
   - Chips area: for each keyword, render a span.chip containing the keyword text and a button with hx-delete="/settings/keywords/{keyword|urlencode}" hx-target="#keywords-section" hx-swap="outerHTML" displaying X
   - Input field: text input with hx-post="/settings/keywords" hx-trigger="keyup[key=='Enter']" hx-target="#keywords-section" hx-swap="outerHTML" hx-include="this" autocomplete="off" placeholder="Type keyword, press Enter"
   - Show count: "N keywords" below the chips
   - Include flash if set

3. Add chip CSS to app.css:
   - .chips { display: flex; flex-wrap: wrap; gap: 0.5rem; margin-bottom: 1rem; }
   - .chip { display: inline-flex; align-items: center; gap: 0.25rem; padding: 0.25rem 0.75rem; background: var(--pico-secondary-background); border-radius: 1rem; font-size: 0.875rem; }
   - .chip-remove { background: none; border: none; cursor: pointer; padding: 0 0.25rem; font-size: 1.1rem; line-height: 1; color: var(--pico-del-color); }
   - .chip-remove:hover { color: var(--pico-color); }

IMPORTANT: Keywords are stored pipe-delimited ("|") in Settings.keywords_csv. Parse with `kw.split("|")`, join with `"|".join(keywords)`. Filter empty strings after split.
  </action>
  <verify>
Navigate to Keywords section. Type "python" + Enter — chip appears. Type "javascript" + Enter — second chip. Click X on "python" — removed. Type "python" again — re-added. Reload page — keywords persist. Try blank input — rejected.
  </verify>
  <done>Keywords render as removable chips. Enter adds, X removes. Pipe-delimited storage. Dedup enforced. Persists across reloads.</done>
</task>

<task type="auto">
  <name>Task 2: Threshold slider + Schedule + Budget sections</name>
  <files>
    app/web/templates/partials/settings_threshold.html.j2
    app/web/templates/partials/settings_schedule.html.j2
    app/web/templates/partials/settings_budget.html.j2
    app/web/routers/settings.py
  </files>
  <action>
1. THRESHOLD SECTION:
   - Route: POST /settings/threshold — validates 0-100, saves match_threshold via set_setting, returns partial with flash
   - Template settings_threshold.html.j2:
     * `<input type="range" name="match_threshold" min="0" max="100" step="5" value="{{ settings.match_threshold }}" oninput="this.nextElementSibling.textContent = this.value + '%'">`
     * `<output>{{ settings.match_threshold }}%</output>`
     * Descriptive labels below slider in a flex row: "Loose (30%)" left, "Moderate (60%)" center, "Strict (85%)" right
     * Save button, form hx-post="/settings/threshold" hx-target="#settings-content"

2. SCHEDULE SECTION:
   - Route: POST /settings/schedule — accepts schedule_enabled (checkbox, interpret missing as false), quiet_hours_start (int 0-23), quiet_hours_end (int 0-23). Saves all three. Returns partial with flash.
   - Template settings_schedule.html.j2:
     * Checkbox/toggle for schedule_enabled: "Enable hourly schedule"
     * Two range inputs for quiet hours:
       - "Quiet hours start": `<input type="range" min="0" max="23" step="1" name="quiet_hours_start" value="{{ settings.quiet_hours_start }}" oninput="...">`
       - "Quiet hours end": `<input type="range" min="0" max="23" step="1" name="quiet_hours_end" value="{{ settings.quiet_hours_end }}" oninput="...">`
       - Display formatted time next to each (e.g., "10:00 PM", "7:00 AM") using a helper or inline JS that converts 0-23 to 12hr format
     * Explanation text: "No applications will be submitted during quiet hours"
     * Save button, form hx-post="/settings/schedule" hx-target="#settings-content"

3. BUDGET SECTION:
   - Route: POST /settings/budget — accepts budget_cap_dollars (float, >= 0). Saves. Returns partial with flash.
   - Template settings_budget.html.j2:
     * Dollar input: `<input type="number" name="budget_cap_dollars" min="0" step="0.01" value="{{ settings.budget_cap_dollars }}" placeholder="0.00">` with a "$" label prefix
     * Explanation: "Set to 0 for no budget limit"
     * Progress bar showing spend vs cap: use HTML5 `<progress>` element or a styled div
       - Label: "${{ '%.2f'|format(settings.budget_spent_dollars) }} / ${{ '%.2f'|format(settings.budget_cap_dollars) }}"
       - If cap is 0, show "No limit set" instead of the bar
       - Bar starts at $0.00 spent (Phase 4 will increment this)
     * Save button, form hx-post="/settings/budget" hx-target="#settings-content"

4. Register all section GETs in the /settings/section/{name} dispatcher (threshold, schedule, budget) to load Settings row and render their respective partials.

NOTE: For the schedule_enabled checkbox in HTML forms — unchecked checkboxes are NOT submitted. Handle by checking if the field is present in form data. Use a hidden input pattern or check explicitly: `schedule_enabled: Optional[str] = Form(None)` and treat None/missing as False, any value as True.
  </action>
  <verify>
Navigate to each section. Threshold: slide to 75%, save — shows "75%", persists on reload. Schedule: enable toggle, set quiet hours 22-7, save — persists. Budget: enter $20.00, save — shows progress bar "$0.00 / $20.00". All existing tests still pass.
  </verify>
  <done>Threshold slider shows percentage + descriptive labels, saves 0-100 value. Schedule toggle + quiet hour sliders work with formatted time display. Budget input + progress bar shows spend vs cap. All persist correctly.</done>
</task>

</tasks>

<verification>
- Keywords: add/remove chips, pipe-delimited storage, dedup, persist
- Threshold: slider 0-100, step 5, labels (Loose/Moderate/Strict), saves
- Schedule: enable/disable toggle, quiet hours 0-23 with formatted display, saves
- Budget: dollar input, progress bar (starts at $0), saves, 0=no limit
- All four sections accessible from sidebar
- All existing tests pass
</verification>

<success_criteria>
- User can manage keywords as visual chips (add via Enter, remove via X)
- User can set match threshold 0-100% with descriptive labels at key points
- User can toggle hourly schedule and configure quiet hours window
- User can set monthly LLM budget cap and see spend-vs-cap progress bar
- All values persist in the Settings table across page reloads
</success_criteria>

<output>
After completion, create `.planning/phases/02-configuration-profile-resume-upload/02-03-SUMMARY.md`
</output>
