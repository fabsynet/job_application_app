---
phase: 03-safe-channel-discovery-dedup-matching
plan: 05
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - app/web/templates/dashboard.html.j2
  - app/web/templates/partials/dashboard_discovery_summary.html.j2
  - app/web/templates/partials/anomaly_banner.html.j2
  - app/web/routers/dashboard.py
  - app/web/templates/run_detail.html.j2
autonomous: true

must_haves:
  truths:
    - "Dashboard shows per-source discovery counts (discovered/matched)"
    - "Anomaly warning banner appears when today < 20% of 7-day rolling average"
    - "Anomaly banner is dismissable and does not reappear until next anomaly"
    - "Run detail view shows per-source discovery/matched counts"
    - "Failed sources show distinct error treatment from anomalies"
  artifacts:
    - path: "app/web/templates/partials/dashboard_discovery_summary.html.j2"
      provides: "Per-source discovery counts summary cards"
      contains: "discovered"
    - path: "app/web/templates/partials/anomaly_banner.html.j2"
      provides: "Dismissable anomaly warning banner"
      contains: "anomaly"
  key_links:
    - from: "app/web/routers/dashboard.py"
      to: "app/discovery/service.py"
      via: "Load latest run stats and anomaly state"
      pattern: "discovery_run_stats|anomaly"
    - from: "app/web/templates/dashboard.html.j2"
      to: "partials/anomaly_banner.html.j2"
      via: "Include if anomaly_warning set"
      pattern: "anomaly_banner"
---

<objective>
Add discovery feedback to the dashboard and run detail view: per-source discovery counts, anomaly warning banners, and error state display.

Purpose: Users need to see discovery results at a glance on the dashboard and drill into per-source stats in run detail. Anomaly detection surfaces issues before they become invisible failures.
Output: Dashboard discovery summary, dismissable anomaly banner, and enriched run detail view.
</objective>

<execution_context>
@C:\Users\abuba\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\abuba\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-safe-channel-discovery-dedup-matching/03-RESEARCH.md
@.planning/phases/03-safe-channel-discovery-dedup-matching/03-01-SUMMARY.md
@.planning/phases/03-safe-channel-discovery-dedup-matching/03-02-SUMMARY.md

@app/web/routers/dashboard.py
@app/web/templates/dashboard.html.j2
@app/web/templates/run_detail.html.j2
@app/web/routers/runs.py
@app/discovery/models.py
@app/discovery/service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add discovery summary and anomaly banner to dashboard</name>
  <files>app/web/routers/dashboard.py, app/web/templates/dashboard.html.j2, app/web/templates/partials/dashboard_discovery_summary.html.j2, app/web/templates/partials/anomaly_banner.html.j2</files>
  <action>
**Dashboard router updates (dashboard.py):**

In the main GET `/` route, add context for discovery summary:
1. Query the most recent completed Run that has discovery counts in `counts` JSON
2. If counts exist, extract per-source stats: `counts.get("source_counts", {})` and totals
3. Check for anomaly state: read `counts.get("anomalies", [])` from the latest run
4. Track dismissed anomalies: use a simple approach -- store dismissed anomaly run_id in a cookie or in Settings. When a new run produces a new anomaly, the banner reappears. Use a cookie `dismissed_anomaly_run_id` for simplicity (no DB change needed).
5. Pass to template: `discovery_summary` (dict with total discovered, matched, per-source breakdown), `anomaly_warnings` (list of warning messages), `show_anomaly_banner` (bool based on cookie comparison)

Add a POST `/dismiss-anomaly` route:
- Sets `dismissed_anomaly_run_id` cookie to current anomaly run_id
- Returns empty response with `hx-swap="delete"` to remove the banner DOM element

**dashboard_discovery_summary.html.j2:**
Create a summary section showing per-source cards (Claude's discretion on layout):
- For each source with stats: show source name, discovered count, matched count
- Total row: "Total: X discovered, Y matched"
- Use compact card/grid layout that fits naturally in the existing dashboard
- Only show if discovery has run at least once (check if discovery_summary is not empty)

**anomaly_banner.html.j2:**
Per user decision and RESEARCH.md pattern:
```html
{% if show_anomaly_banner and anomaly_warnings %}
<article id="anomaly-banner" style="border: 2px solid #c90; background: #ffd; padding: 1em; margin-bottom: 1em;">
  {% for warning in anomaly_warnings %}
  <p><strong>Low discovery alert:</strong> {{ warning }}</p>
  {% endfor %}
  <button hx-post="/dismiss-anomaly" hx-swap="delete" hx-target="#anomaly-banner" class="secondary outline" style="float:right;">Dismiss</button>
  <div style="clear:both;"></div>
</article>
{% endif %}
```

Per user decision: "Anomaly banners are dismissable -- won't reappear until a new anomaly is detected on a subsequent run"

**dashboard.html.j2 updates:**
- Include `partials/anomaly_banner.html.j2` at the top of the dashboard content
- Include `partials/dashboard_discovery_summary.html.j2` after the existing status section
- Both use `{% include %}` so they render conditionally based on context
  </action>
  <verify>
Start the app, navigate to dashboard. If no discovery runs yet, no summary or banner shows. After a discovery run with anomalies, banner appears and is dismissable.
`pytest tests/ -x -q` passes.
  </verify>
  <done>Dashboard shows per-source discovery counts after each run. Anomaly warning banner appears for low discovery counts, is dismissable, and reappears only on new anomalies.</done>
</task>

<task type="auto">
  <name>Task 2: Enrich run detail view with per-source discovery stats</name>
  <files>app/web/templates/run_detail.html.j2, app/web/routers/runs.py</files>
  <action>
**runs.py updates:**

In the GET `/runs/{run_id}` route, add:
1. Query `DiscoveryRunStats` rows for this run_id
2. Join with Source table to get source slug/type for display
3. Pass `discovery_stats` list to template context

**run_detail.html.j2 updates:**

Add a "Discovery Results" section to the run detail page:
- Section header "Discovery"
- Table showing per-source breakdown:
  | Source | Type | Discovered | Matched | Status |
  - Status column: "OK" (green) or error message (red) from DiscoveryRunStats.error
- Total row at bottom
- Only show this section if discovery_stats is not empty

Per user decision: "Per-source discovery counts (discovered/matched) shown in both dashboard summary and run detail view"

Show source fetch errors distinctly per user decision: "Failed source fetches (404, timeout) are a SEPARATE error state from anomalies -- red 'Error' badge on the source in the sources list, distinct error banner"
- Sources with errors show red "Error" badge + error message
- Sources with low counts but no error show normal (anomaly is dashboard-level, not run-detail-level)
  </action>
  <verify>
Navigate to a run detail page. If the run included discovery, per-source stats table appears showing discovered/matched counts per source and any errors.
`pytest tests/ -x -q` passes.
  </verify>
  <done>Run detail view shows per-source discovery counts (discovered, matched) with error status for failed sources. Dashboard and run detail both display discovery feedback per user decision.</done>
</task>

</tasks>

<verification>
- Dashboard shows discovery summary cards after at least one discovery run
- Anomaly banner appears for sources with < 20% of rolling average (when >= 3 data points)
- Dismissing anomaly banner sets cookie; banner does not reappear until next anomaly
- Run detail page shows per-source discovery table with discovered/matched/error
- Error badges (red) are visually distinct from anomaly warnings (yellow)
</verification>

<success_criteria>
Users can see per-source discovery counts on both the dashboard and run detail view. Anomaly warnings surface as dismissable yellow banners on the dashboard. Failed source fetches show as distinct red error states.
</success_criteria>

<output>
After completion, create `.planning/phases/03-safe-channel-discovery-dedup-matching/03-05-SUMMARY.md`
</output>
