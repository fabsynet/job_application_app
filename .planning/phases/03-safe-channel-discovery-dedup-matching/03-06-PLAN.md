---
phase: 03-safe-channel-discovery-dedup-matching
plan: 06
type: execute
wave: 4
depends_on: ["03-02", "03-03", "03-04", "03-05"]
files_modified:
  - tests/test_phase3_discovery.py
  - tests/test_phase3_integration.py
autonomous: true

must_haves:
  truths:
    - "All 8 active Phase 3 requirements are covered by tests (DISC-01..03, DISC-05..06, MATCH-01..03; DISC-04 deferred)"
    - "Dedup prevents duplicate jobs across runs and sources"
    - "Scoring produces correct results for partial, case-insensitive matching"
    - "Pipeline integrates with scheduler safety envelope (kill-switch, rate-limit)"
    - "Source validation rejects invalid slugs"
    - "Anomaly detection skips sources with fewer than 3 data points"
  artifacts:
    - path: "tests/test_phase3_discovery.py"
      provides: "Unit tests for fetchers, scoring, dedup, service"
      min_lines: 100
    - path: "tests/test_phase3_integration.py"
      provides: "Integration tests for pipeline, routes, end-to-end flows"
      min_lines: 80
  key_links:
    - from: "tests/test_phase3_discovery.py"
      to: "app/discovery/scoring.py"
      via: "score_job and job_fingerprint tests"
      pattern: "score_job|job_fingerprint"
    - from: "tests/test_phase3_integration.py"
      to: "app/discovery/pipeline.py"
      via: "Pipeline integration with mocked HTTP"
      pattern: "run_discovery"
---

<objective>
Write comprehensive unit and integration tests covering all Phase 3 requirements: fetchers, scoring, dedup, source management, pipeline integration, UI routes, and anomaly detection.

Purpose: Prove that all 8 active Phase 3 requirements (DISC-01..03, DISC-05..06, MATCH-01..03) are satisfied. DISC-04 (web search) is explicitly deferred.
Output: Two test files providing full coverage of Phase 3 functionality.
</objective>

<execution_context>
@C:\Users\abuba\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\abuba\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-safe-channel-discovery-dedup-matching/03-RESEARCH.md
@.planning/phases/03-safe-channel-discovery-dedup-matching/03-02-SUMMARY.md
@.planning/phases/03-safe-channel-discovery-dedup-matching/03-03-SUMMARY.md
@.planning/phases/03-safe-channel-discovery-dedup-matching/03-04-SUMMARY.md
@.planning/phases/03-safe-channel-discovery-dedup-matching/03-05-SUMMARY.md

@app/discovery/fetchers.py
@app/discovery/scoring.py
@app/discovery/service.py
@app/discovery/pipeline.py
@app/web/routers/jobs.py
@app/web/routers/sources.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit tests for discovery modules</name>
  <files>tests/test_phase3_discovery.py</files>
  <action>
Create `tests/test_phase3_discovery.py` with unit tests covering:

**detect_source tests:**
- Greenhouse URL: `https://boards.greenhouse.io/stripe` -> ("greenhouse", "stripe")
- Greenhouse API URL: `https://boards-api.greenhouse.io/v1/boards/stripe/jobs` -> ("greenhouse", "stripe")
- Lever URL: `https://jobs.lever.co/stripe` -> ("lever", "stripe")
- Ashby URL: `https://jobs.ashbyhq.com/stripe` -> ("ashby", "stripe")
- Plain slug: `stripe` -> ("unknown", "stripe")
- Invalid input: raises ValueError

**score_job tests (MATCH-01):**
- Case-insensitive: "Python" in desc matches keyword "python"
- Partial matching: "python3" in desc matches keyword "python"
- Multi-word partial: "Python/Django" matches keyword "python"
- Score calculation: 2 of 4 keywords matched -> 50
- Empty keywords -> (0, [], [])
- No matches -> (0, [], keywords)
- All matched -> (100, keywords, [])

**job_fingerprint tests (DISC-06):**
- Same inputs produce same hash
- Different URL query params produce same hash (query stripped)
- Case insensitive: "Stripe" and "stripe" produce same hash
- Trailing slash stripped: url/ and url produce same hash
- Different jobs produce different hashes

**Fetcher response parsing tests (DISC-01, DISC-02, DISC-03):**
Mock the HTTP responses (use httpx mock or unittest.mock) to test parsing:
- Greenhouse: mock `{"jobs": [...]}` response, verify normalized output
- Lever: mock flat array `[{...}]` response (NOT wrapped), verify normalized output
- Lever: verify posted_date is None
- Ashby: mock `{"jobs": [...]}` with descriptionPlain and descriptionHtml, verify both extracted
- Greenhouse: verify content=true param is passed

**strip_html tests:**
- Removes tags: `<p>Hello</p>` -> "Hello"
- Handles nested tags
- Preserves text content

**Anomaly detection tests:**
- Fewer than 3 data points: skip anomaly (return None/False)
- Normal count (above 20% of average): no anomaly
- Low count (below 20% of average): anomaly detected
  </action>
  <verify>
`pytest tests/test_phase3_discovery.py -v` — all tests pass.
  </verify>
  <done>Unit tests cover detect_source, score_job (case-insensitive + partial), job_fingerprint (canonicalization), all three fetcher parsers, strip_html, and anomaly detection edge cases.</done>
</task>

<task type="auto">
  <name>Task 2: Integration tests for pipeline and routes</name>
  <files>tests/test_phase3_integration.py</files>
  <action>
Create `tests/test_phase3_integration.py` with integration tests. Follow existing integration test patterns from Phase 1/2 (lifespan-driven, tmp_path data dirs).

**Pipeline integration tests:**
- Mock httpx responses for all three ATS APIs
- Run `run_discovery` with mocked HTTP, verify:
  - Jobs persisted to DB with correct normalized fields (DISC-05)
  - Dedup: running pipeline twice with same mock data creates no duplicates (DISC-06)
  - Scoring: jobs get correct scores based on keywords (MATCH-01)
  - Status: jobs at/above threshold get "matched", below get "discovered" (MATCH-02)
  - Score stored in DB alongside job record (MATCH-03)
  - matched_keywords stored pipe-delimited (MATCH-03)
  - DiscoveryRunStats rows created per source
  - Source fetch errors: one source failing does not prevent other sources from completing
  - Source error status updated in DB

**Route integration tests:**
- GET /jobs: returns 200 with jobs table
- GET /jobs?sort=score&dir=desc: returns sorted results
- GET /jobs/{id}/detail: returns inline detail partial with keyword breakdown
- POST /jobs/{id}/queue: changes status to "queued"
- GET /settings/sources: returns sources section
- POST /settings/sources with valid slug: mock validation to succeed, verify source created
- POST /settings/sources with invalid slug: mock validation to fail, verify error returned, no source created
- POST /settings/sources/{id}/toggle: toggles enabled state
- DELETE /settings/sources/{id}: removes source

**Dashboard integration tests:**
- GET / after discovery run: discovery summary present in response
- Anomaly banner appears when appropriate
- POST /dismiss-anomaly: sets cookie, banner hidden

**Cross-cutting:**
- Verify all existing Phase 1+2 tests still pass (run full suite)
  </action>
  <verify>
`pytest tests/test_phase3_integration.py -v` — all tests pass.
`pytest tests/ -v` — full suite passes (Phase 1 + 2 + 3 tests all green).
  </verify>
  <done>Integration tests verify pipeline end-to-end (fetch, normalize, dedup, score, persist), all CRUD routes for sources and jobs, dashboard discovery summary, and anomaly banner. Full test suite green.</done>
</task>

</tasks>

<verification>
- `pytest tests/ -v` — full suite passes
- Coverage: DISC-01 (greenhouse fetch), DISC-02 (lever fetch), DISC-03 (ashby fetch), DISC-05 (normalization), DISC-06 (dedup), MATCH-01 (scoring), MATCH-02 (threshold status), MATCH-03 (score stored + displayed)
- DISC-04 explicitly NOT tested (deferred — web search discovery)
- No regressions in Phase 1/2 tests
</verification>

<success_criteria>
All 8 active Phase 3 requirements are verified by tests. Full test suite (Phase 1 + 2 + 3) passes. DISC-04 is explicitly deferred with no test coverage expected.
</success_criteria>

<output>
After completion, create `.planning/phases/03-safe-channel-discovery-dedup-matching/03-06-SUMMARY.md`
</output>
